<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="net.sourcefusion.agiletools.sprintcalendar"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SprintCalendarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
        android:name="DATABASE"
        android:value="SprintCalendar.db"/>
        <!--<meta-data-->
            <!--android:name="DATABASE"-->
            <!--android:value="sprint_calendar_2.db"/>-->
        <meta-data
            android:name="VERSION"
            android:value="14"/>
        <meta-data
            android:name="QUERY_LOG"
            android:value="true"/>
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="net.sourcefusion.agiletools.sprintcalendar.persisting.sugar"/>


        <activity
            android:name="net.sourcefusion.agiletools.sprintcalendar.MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
